# Generated by xml_to_baseline.py — review before use in audit.py
# Fields with type: numeric and exact 'expected' values were inferred
# from the known-good config.  Add min/max or --tolerance to widen them.

version: "1.0"
description: "Production Security Center baseline — Q2 2026"
config_checks:
  config.smtpHost:
    type: string
    expected: "smtp.example.com"
  config.smtpPort:
    type: numeric
    min: 20
    max: 30
  config.smtpEncryption:
    type: string
    expected: "TLS"
  config.smtpFromAddress:
    type: string
    expected: "noreply@example.com"
  config.smtpFromName:
    type: string
    expected: "Tenable Security Center"
  config.smtpAuthType:
    type: string
    expected: "NONE"
  config.ldapEnabled:
    type: boolean
    expected: true
  config.ldapHost:
    type: string
    expected: "ldap.example.com"
  config.ldapPort:
    type: numeric
    min: 508.8
    max: 763.2
  config.ldapEncryption:
    type: string
    expected: "LDAPS"
  config.samlEnabled:
    type: boolean
    expected: false
  config.sessionTimeout:
    type: numeric
    min: 24
    max: 36
  config.allowUserManagement:
    type: boolean
    expected: true
  system_info.licenseStatus:
    type: string
    expected: "Valid"
  system_info.licenseType:
    type: string
    expected: "Subscription"
  system_info.version:
    type: string
    expected: "6.3.0"
  status.feedStatus:
    type: string
    expected: "Current"
  status.updateRunning:
    type: boolean
    expected: false
scope:
  cidrs:
  - "192.168.0.0/16"
  - "10.10.0.0/24"
  - "172.16.50.0/24"
  ips:
  - "192.168.1.50"
  - "10.10.0.100"
  hostnames:
  - "server01.corp.example.com"
